name: quiz_app
description: "A new Flutter project for Integrative Programming 2 Final."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.1 
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.1.0 
  flutter_secure_storage: ^9.2.2
  cached_network_image: ^3.3.1
  path_provider: ^2.1.2
  google_fonts: ^6.2.1
  
  # BETTER ALTERNATIVE: Fast, code-based animations
  confetti: ^0.7.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Note: Assets section removed to fix the "directory not found" error